{
  "name": "mcp-gateway",
  "version": "1.0.0",
  "description": "Routing gateway for MCP servers — handles OAuth discovery and proxies to individual MCP services",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts src/setup.ts --format esm --sourcemap",
    "start": "node dist/index.js",
    "setup": "node dist/setup.js"
  },
  "dependencies": {
    "@rollup/rollup-linux-arm64-gnu": "^4.59.0",
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.19.33",
    "tsup": "^8.0.0",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}
